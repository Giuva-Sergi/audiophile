import Overlay from "../components/Overlay";
import CartContainer from "./CartContainer";

function Cart() {
  return (
    <Overlay>
      <CartContainer />
    </Overlay>
  );
}

export default Cart;
